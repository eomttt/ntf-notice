import { Layout } from 'components/Layout';
import type { NextPage } from 'next';

const Home: NextPage = () => (
  <Layout>
    <h1 className="w-full text-center text-6xl font-bold text-black-500 pt-20">기능추가필요</h1>
  </Layout>
);

export default Home;
